<% if (title) { %>
    <h1 class="text-3xl lg:text-4xl font-black tracking-tight uppercase text-[var(--primary)] mb-3 mt-10 ml-10">
        <%= title  %>
    </h1>
<% } else { 
    res.redirect('/')
} %>

<%- dataDetails %>
 